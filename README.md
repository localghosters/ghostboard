# GhostBoard

A deliberately small LocalGhosters starter repo.

## Features

- 100% static — no backend required
- Works directly on GitHub Pages
- Notes are stored in the browser with `localStorage`
- Random ghost generator

## Run locally

Open `index.html` in a browser.

## GitHub Pages

This repository can be deployed as a static GitHub Pages site with no server.
